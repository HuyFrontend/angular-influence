import { CurrencyFixedTwoPipe } from './currency-fixed-two.pipe';

describe('CurrencyFixedTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFixedTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
