import { NospaceValidatorDirective } from './nospace-validator.directive';

describe('NospaceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NospaceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
